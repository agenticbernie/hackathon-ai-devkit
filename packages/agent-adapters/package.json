{
  "name": "@hadk/agent-adapters",
  "version": "2.0.7",
  "description": "Agent adapter generation for HADK — Claude Code, Codex, OpenCode instruction files from one canonical protocol",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@hadk/core": "workspace:*",
    "@hadk/state-store": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
